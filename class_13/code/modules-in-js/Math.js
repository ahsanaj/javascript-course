const add = (x, y) => x + y;
const subtract = (x, y) => x - y;
const PI = Math.PI;

// enhanced object literals - key and value names are same, write them once
module.exports = {
    add,
    subtract,
    PI
};